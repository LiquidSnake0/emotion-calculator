# Session studio du 26 septembre 2026 — le premier set réel

Un studio prêté pour enregistrer un mix. Le set se joue **sur clé USB rekordbox**, sur deux
CDJ et une **DJM-750MK2**. C'est la première fois que le moteur entend une vraie table, de
vrais faders, un vrai DJ qui enchaîne dix disques. Tout ce qui précède a été mesuré sur des
fondus fabriqués ; ce document dit ce qu'on teste, comment, et ce qu'on attend — **avant**
de le faire, pour ne pas se noter soi-même après coup.

Deux objectifs, dans cet ordre :

1. **Ramener la matière** : le WAV multipiste de la table, les images que le moteur a
   écrites (`.pak`), les notes du DJ. Si le direct rate, la matière suffit à tout rejouer.
2. **Voir le moteur en direct** sur un vrai set : capture, tempo, relais à chaque passage,
   cue qui change de voie, fenêtre qui suit le fader.

## Le montage

```
CDJ 1 ──► voie 1 ─┐                       USB (5 paires stéréo, 48 kHz, 24 bits)
CDJ 2 ──► voie 2 ─┤ DJM-750MK2 ═══════════════════════════════════════════════► portable
                  │   paire 1 = voie 1 avant fader   (« Control Tone »)          │
                  │   paire 2 = voie 2 avant fader                              │ parecord → WAV
                  │   paire 5 = Rec Out (master)                                │ moteur  → anneau → .pak
                  └─► salle                                                     │ fenêtre
```

Le casque ne passe pas par l'USB : **le cue du moteur est la voie avant fader du deck qui
se prépare**, et le moteur sait laquelle par l'alternance (`CueAlternant`, ci-dessous).
C'est plus propre que le casque, où le potard MIX laisse toujours passer du master.

Le portable est cette machine (HP EliteBook x360 1030 G3, Manjaro, noyau 6.1) : la
DJM-750MK2 y est connue du noyau (quirk ALSA depuis Linux 5.14), rien à installer. Il faut
un **câble USB-B**.

## Ce qui a été préparé (commits `a041ecc` et suivant)

| pièce | rôle | vérifié |
|---|---|---|
| `outils/studio.sh` | état / `--verif` / session : remap des 5 paires, WAV, moteur, `.pak`, fenêtre | syntaxe, état sans table ; **jamais vu la table** |
| `probe enregistre <pak>` | l'anneau → fichier, à rejouer avec `probe rejoue` ou `rejouer.sh` (renderer) | 281 images en 6 s, 0 manquée |
| `outils/terrain.sh` | notes du DJ datées à l'heure de la machine | — |
| `outils/cue.sh` | la fiche du disque posé (`/deck/cue`) et le `take`, notés aussi dans les notes | — |
| crate, onglet **Set** | l'ordre décidé d'avance, le crate suit : `play`, `cue`, `take` envoyés au moteur avec BPM joué et clé transposée | bout en bout sur cette machine, `deck.jsonl` : play → cue → take |
| `DeckJournal` | chaque commande du crate, à l'heure de la machine et à l'horloge des images | idem |
| `outils/fiches_rekordbox.py` | BPM battement par battement des 1 943 morceaux de la clé (grille PQTZ) | index écrit |
| `CueAlternant` | le cue passe sur l'autre voie après chaque relais ; filet si la voie est dans le master | 8 tests |
| `MachineRelais` | les quatre temps du relais, sortis de la boucle de lecture | 9 tests, **corrige « un seul relais par session »** |
| chaîne e-c → e-r | `rejouer.sh` sur un `.pak` existant | 235 images, 0 trou |

Le défaut corrigé mérite d'être dit : après un retrait, rien ne remettait la machine du
relais à zéro. Le premier passage de la soirée se relayait, tous les suivants passaient sans
accueil ni retrait. La mesure de référence (`relais.py`) ne fait qu'un fondu et ne pouvait
pas le voir. Un set de dix disques l'aurait montré à la troisième minute.

## Protocole, avant de jouer (10 minutes)

1. Brancher l'USB, lancer `./outils/studio.sh` : la carte DJM et sa source doivent
   apparaître, avec cinq paires et leurs commutateurs.
2. Mettre un disque sur chaque CDJ, en lecture, faders **en bas**. `./outils/studio.sh --verif` :
   choisit l'entrée de chaque voie à l'oreille (LINE / CD-LINE / DIGITAL) et donne le niveau
   de chaque paire en dBFS. Attendu : paires 1 et 2 au-dessus de −40 dBFS, paire 5 à
   « rien » ou très bas (faders en bas), paires 3 et 4 à rien.
3. Monter le fader 1, relancer `--verif` : la paire 5 doit maintenant porter du signal.
4. `./outils/studio.sh set-studio <bpm du premier disque> [camelot]` : la fenêtre s'ouvre,
   le journal du moteur dit `cue alternant : voie 1 = djm_2, voie 2 = djm_1, le cue commence
   sur la 1` (ses « voies » sont les deux candidates, dans l'ordre donné). **Le premier
   disque joue sur la voie 1 de la table, le premier disque préparé est sur la voie 2.** Si
   le set commence dans l'autre sens, `STUDIO_CUE=1` ; et si on s'est trompé, le filet
   permute en trois secondes (une ligne `filet : …` dans le journal, c'est prévu).
5. Deuxième terminal : `./outils/terrain.sh set-studio`. **Le crate sur l'iPhone** : dans
   `~/Documents/crate`, `npm run dev -- --host 0.0.0.0`, puis sur le téléphone
   `http://<ip du portable>:5173/crate/` (l'ip est imprimée par `studio.sh`, c'est celle du
   partage de connexion ; en http, pas la PWA GitHub Pages qui est en https et ne peut pas
   appeler un moteur en http). Onglet **Set**, moteur = `http://<ip>:5099`, set = « Mix 2 »
   (importer `crate-mix2.json` dans Bibliothèque si ce navigateur ne l'a pas). Puis
   « démarrer », et à chaque disque « je le cale au casque » / « il passe en salle » : ce
   sont `/deck/play`, `/deck/cue`, `/deck/take`, journalisés côté moteur dans
   `<session>-deck.jsonl` à l'heure de la machine **et** à l'horloge des images. Plan B :
   `./outils/cue.sh set-studio …` à la main.
6. Vérifier que `<session>-table.wav` grossit (~1,4 Mo/s pour dix canaux) et que
   `<session>-pak.log` compte des images.

## T0 — l'échauffement, avant le set

Deux ou trois disques du crate vinyle (ceux qui sont aussi sur la clé : *The Era of
Information* y est), joués à la suite, **avec l'écran Live du crate** — les probabilités,
pas l'ordre imposé. But : voir que la fiche arrive, que rien ne fuit d'un disque à l'autre,
avant de lancer le set. Gestes : choisir par quoi on démarre (`play`), taper la ligne du
candidat choisi (`cue`), « il passe en salle » quand le fader est en haut (`take`).

| vérifier | où | attendu |
|---|---|---|
| chaque geste répond 200 | ligne « moteur · … » sous le disque courant | 0 « injoignable » |
| le moteur a la bonne fiche | `curl <ip>:5099/deck` | `playing` = le disque en salle, `cued` = celui au casque, BPM = ancrage, Camelot = transposée |
| pas de fuite | `deck.jsonl` | une ligne par geste, dans l'ordre, chacune avec le bon titre |
| le relais suit | journal moteur | `cue → voie N` après chaque passage ; pas de « filet » après le premier |

Si T0 échoue sur le réseau (partage de connexion), on passe au plan B `cue.sh` et le set
se joue quand même.

## Les cas de test

| # | ce qu'on teste | comment | attendu | où lire |
|---|---|---|---|---|
| T1 | **La capture** : les cinq paires arrivent, à part | `--verif`, puis le WAV | paires 1, 2, 5 avec signal ; WAV 10 canaux 48 kHz 24 bits, sans coupure | `--verif`, `soxi`/`ffprobe` sur le WAV |
| T2 | **Le tempo en direct** sur le master | fiche donnée par `cue.sh` | tempo publié en moins de 15 s (repère 15,1 s sur fichier), à ±1 BPM de `BPM rekordbox × (1 + pitch)` ; `bpm` jamais à 0 plus de 2 s hors passage | `<session>-moteur.log`, fenêtre (gris = valeur gardée) |
| T3 | **Le cue change de voie** à chaque passage | rien à faire, jouer | une ligne `cue → voie N (apres le relais…)` par passage ; **filet 0 fois**, ou 1 fois au tout début si le sens de départ était faux | journal moteur |
| T4 | **Le relais entier à chaque passage** | jouer | phases 1 → 2 → 3 puis libre à chaque fondu, autant de retraits que de passages ; à l'écran, les cases P1 s'éteignent pendant que P2 s'allument, rien ne saute de côté | journal moteur, `.pak` (octet 60 : relais) |
| T5 | **La fiche compte** | un passage sans `cue.sh` (contrôle) | ce passage-là : tempo plus lent à se poser ou faux (référence : 43 % de justesse sans fiche, 99 % avec) | journal moteur |
| T6 | **Le `.pak`** est complet | rien à faire | ≈ 47 images par seconde de set, `0 manquees` dans `<session>-pak.log` ; `rejouer.sh` : `0 trou(s)` | `<session>-pak.log`, renderer |
| T7 | **La vérité terrain** est lisible | `terrain.sh`, le crate (ou `cue.sh`) | une ligne `cue` puis `take` par disque dans `deck.jsonl`, avec `imageMs` ; à la maison, écart entre le `take` et la phase 3 du moteur < 10 s | `<session>-deck.jsonl`, `<session>-terrain.tsv` |
| T9 | **Le crate parle au moteur** sur le réseau du téléphone | onglet Set du crate | chaque geste répond 200 dans le journal de l'écran ; `deck.jsonl` a une ligne par geste ; 0 « injoignable » | écran Set, `<session>-deck.jsonl` |
| T8 | **Le fader réel**, jamais mesuré | à la maison, sur le WAV | position du fader de chaque voie par différence master − voies avant fader ; `BlendEstimator` comparé à cette vérité (jusqu'ici : 0,71–0,85 sur des fondus fabriqués) | `outils/relais.py`, à étendre |

**Succès de la session** : T1 et T6 tenus (la matière est là, complète), T3 et T4 tenus sur
la majorité des passages. **Échec instructif** : T3 ou T4 faux — on a le WAV pour comprendre
pourquoi, passage par passage. **Échec sec** : T1 faux (pas de capture) — alors le studio
enregistre le master de son côté et on rejoue le moteur dessus, sans cue.

## Ce qu'on ne saura pas demain

- Le relais en direct est testé avec un cue qui est **la voie brute**, pas le casque : ce que
  le DJ entend n'entre pas dans le moteur. Ce n'est pas un manque, c'est le montage.
- Le pitch et l'EQ sur un vrai fader ne se jugent qu'à la maison (T8).
- La tonalité (Camelot) des morceaux de la clé demande `rekordbox-pdb` (GitHub, pas sur
  PyPI) ; sans elle `cue.sh` part sans clé, et le degré par source reste « inconnu ».

## Risques connus, et le plan B

| risque | signe | plan B |
|---|---|---|
| PulseAudio ne voit pas la table | `studio.sh` : « aucune DJM vue » | `pactl list cards` ; profil « multichannel-input » à activer ; à défaut, le studio enregistre le master, on rejoue à la maison |
| `parec` rend 0 octet | `--verif` : « rien » partout | vu depuis mon shell (bac à sable) ; dans son terminal à vérifier **avant** de partir |
| trois analyseurs en direct (master + deux voies) | CPU, trous dans le `.pak` | `STUDIO_CUE=2` seul (`Signal__CueDevice=djm_2`) : un cue fixe, le relais un passage sur deux |
| le sens de départ du cue est faux | `filet : la voie 1 est dans le master` dans les 3 s | rien à faire, c'est prévu ; sinon relancer avec les voies inversées |
| le WAV remplit le disque | 134 Go libres, ~5 Go par heure | rien |

## Ce qu'on ramène, et ce qu'on en fait

`~/.cache/emotion-emulator/studio/<session>/` : `-table.wav`, `.pak`, `-moteur.log`,
`-deck.jsonl`, `-terrain.tsv`, `-pak.log`, plus `~/.cache/emotion-emulator/fenetre.log`.
Trois horloges, une seule référence : le `.pak` porte `TimeMs` (l'horloge des images), le
`deck.jsonl` porte `imageMs` (la même) et `epochMs`, les notes portent `epoch_ms`. Tout se
recale sur l'horloge des images, qui est celle que le rendu reçoit.

À la maison, dans l'ordre : (1) `rejouer.sh` sur le `.pak` — le renderer lit le set entier
tel que le GPU l'aurait reçu ; (2) le WAV découpé par paire, `run.sh fichier` sur le master
seul pour comparer au direct ; (3) T8, le fader réel ; (4) le relais rejoué phase par phase
avec le bon cue à chaque passage, pour noter le direct contre le rejeu.
